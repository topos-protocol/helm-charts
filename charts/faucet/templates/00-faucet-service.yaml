apiVersion: v1
kind: Service
metadata:
  name: faucet
  labels:
    app: faucet
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ports.http }}
      targetPort: http
      protocol: TCP
  selector:
    app: faucet
  sessionAffinity: ClientIP
